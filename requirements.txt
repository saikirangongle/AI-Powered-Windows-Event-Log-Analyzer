# Core HTTP client for Gemini API requests
requests>=2.32.5

# JSON schema validation (for settings/event schemas)
jsonschema>=4.21.1

# Tkinter is included with most Python installations.
# On Linux, users may need to install python3-tk manually.

# Useful utilities
pillow>=12.0.0            # Optional: images, icons if needed
python-dotenv>=1.2.1      # To load .env files

# Testing
pytest>=9.0.1
pytest-cov>=7.0.0

# Linting / Formatting (optional but recommended)
flake8>=7.3.0
black>=25.11.0

# No OpenAI / other providers — Gemini only